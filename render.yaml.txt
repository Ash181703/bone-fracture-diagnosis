services:
  - type: web
    name: bone-fracture-diagnosis
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bone_fracture_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    staticPublishPath: static
